<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Category" parent="@android:style/TextAppearance.Widget.TextView">
        <item name="android:gravity">center</item>
        <item name="android:layout_weight">1</item>
        <item name="android:background">@drawable/item_category</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:layout_height">30dp</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:textColor">#000000</item>
        <item name="android:elevation">2dp</item>
    </style>

    <style name="CategorySelected" parent="@android:style/TextAppearance.Widget.TextView">
        <item name="android:gravity">center</item>
        <item name="android:layout_weight">1</item>
        <item name="android:background">@drawable/item_category_selected</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:layout_height">30dp</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:textColor">#ffffff</item>
    </style>

    <style name="CustomButtonGreen" parent="@android:style/TextAppearance.Widget.Button">
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/ripple_button_green</item>
        <item name="android:textColor">#ffffff</item>
    </style>

    <style name="CustomButtonOrange" parent="@android:style/TextAppearance.Widget.Button">
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/ripple_button_orange</item>
        <item name="android:textColor">#ffffff</item>
    </style>

    <style name="CustomButtonBlue" parent="@android:style/TextAppearance.Widget.Button">
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/ripple_button_blue</item>
        <item name="android:textColor">#ffffff</item>
    </style>


</resources>
